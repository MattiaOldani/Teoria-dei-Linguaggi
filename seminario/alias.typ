// Setup


// Alias

// State complexity

#let nsc(x) = {
  let nscop = math.class(
    "unary",
    $"nsc"$,
  )
  $nscop(#x)$
}

#let svsc(x) = {
  let svscop = math.class(
    "unary",
    $"svsc"$,
  )
  $svscop(#x)$
}
