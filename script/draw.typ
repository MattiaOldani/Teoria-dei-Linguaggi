#set page(height: auto, width: auto, margin: 0.2cm)

#import "@preview/fletcher:0.5.5": diagram, node, edge

#align(center)[
  #diagram(
    node-stroke: .1em,
    spacing: 4em,
    // debug: 2,

    // node((0, 0), ``, radius: 1.25em, fill: white),

    // edge((0, 0), (1, 0)),
  )
]
